# Local credentials and environments
.env
.env.*
!.env.example
*.pem
*.key
.venv/
node_modules/
dist/
__pycache__/
*.pyc
.DS_Store
Thumbs.db
# Real student records and scratch files belong outside Git
data/private/
data/raw/
uploads/
work/
*.log

# 스킬 환경
# .agents/skills/ 는 커밋한다 — 다른 기기에서 같은 환경으로 작업하기 위함.
# .claude/skills/ 는 .agents/skills/ 를 가리키는 절대경로 심볼릭 링크라
# 기기마다 달라진다. 커밋하지 않고 스킬 설치 도구가 다시 만들게 둔다.
.claude/skills/

# 배포 원본·바이너리 문서는 커밋하지 않는다 (내용은 md로 정리해 보관)
# 저장소 원칙: CLAUDE.md 9절
*.hwp
*.hwpx
*.pdf
*.xlsx
*.xls
*.doc
*.docx
*.ppt
*.pptx
*.zip
원본자료/
attachments/
